# AMP v1.0.0

首个稳定版本发布。本次版本经过全面代码审计与修复，解决了原仓库中 README 宣传与实际实现严重脱节的问题，并补齐了多项安全与核心功能。

## 🎉 主要变更

### 🔴 Critical Bug 修复
- **`npm run start` 现可正常运行**：原先直接用 `node` 执行 `.ts` 文件会失败，现改用 `tsx` 并提供 `start:prod` 走编译产物路径。
- **作用域隔离真正实现**：`MemoryScope`（userId / sessionId / agentId）现强制过滤，修复了跨用户数据可互相读取的安全漏洞（TypeScript 与 Python 双端）。
- **构造器异步降级竞态**：修复 `new AMPCore({redisUrl})` 后立即调用 store/retrieve 会因 Redis 未连上而抛错的问题。
- **metadata 合并顺序**：服务器权威字段（timestamp / lastAccessedAt）不再被用户传入值覆盖。
- **retrieve 不再污染存储**：返回深拷贝，score 不再被持久化。
- **Redis `RETURN` 语法错误**：修复查询语法导致检索失败的问题。
- **Redis `KEYS` → `SCAN`**：避免大数据量下阻塞 Redis。
- **id 冲突检测**：显式抛错而非静默覆盖。
- **minImportance=0 边界**：修复 falsy 判断导致过滤被跳过。

### 🛡️ 安全加固
- **MCP/REST 认证**：移除硬编码默认 token `default-dev-token`，生产环境 (`NODE_ENV=production`) 启动时强制要求 `AMP_API_TOKEN` 环境变量。
- **常量时间 token 比较**：使用 `crypto.timingSafeEqual` 防时序侧信道。
- **CORS 限制来源**：默认仅允许 localhost 与 chrome-extension 来源。
- **PUT 端点 Zod 校验**：修复任意字段可注入 metadata 的问题。
- **Body 大小限制**：`express.json({ limit: '1mb' })`。
- **userId 不可由请求体伪造**：改从 `AMP_API_USER_ID` 环境变量派生。

### ✨ 新功能
- **SQLiteStorageProvider**：基于 SQLite 的真实索引/事务后端，避免 File 路径每次 retrieve 全量重写文件的问题。
- **艾宾浩斯遗忘曲线 / 动态重要性**：30 天半衰期 + 访问频次增益（封顶 +0.3），importance 不再写入后永不变化。
- **AES-256-GCM 静态文件加密**：`FileStorageProvider` 支持可选 `encryptionKey` 加密本地文件。
- **审计日志**：`AuditLogger` 追加写 JSON 审计条目，覆盖 store/retrieve/update/delete。
- **自定义 provider 注入**：`AMPConfig.provider` 字段，符合"插件化存储"定位。
- **Chrome 扩展阅读偏好捕获**：scrollDepth + dwellTime，dwell>30s && scroll>50% 时存为记忆。
- **Chrome 扩展频繁搜索捕获**：通过 `chrome.history` + `chrome.alarms`，每 30 分钟扫描搜索引擎查询，2+ 次出现存为记忆。

### 📦 打包与 CI
- **ESLint 配置统一**：删除 legacy `.eslintrc.js`，仅保留 `eslint.config.mjs` (flat config)。
- **CI 补齐 lint 步骤** + `black --check` + Python 3.8-3.12 测试矩阵。
- **Dockerfile 新建** + docker-compose 修复（新增 amp 服务并接入 REDIS_URL）。
- **`tsconfig.mcp.json` 新建**：mcp/ 现纳入构建/类型检查。
- **Python setup.py 修复**：author/URL 从 `OpenClaw Team` / `openclaw/amp` 改为正确的 `ai-nurmamat` / `ai-nurmamat/AMP`。
- **清理陈旧残留**：删除 `amp_memory.egg-info`（amp-memory v1.0.0 历史残留）与已提交的运行时 `amp_memory.json`。
- **`.gitignore` 完善**：覆盖 `dist/`、`*.egg-info/`、`amp_memory.json`、`coverage/` 等。

### 📝 文档
- **README 修正**：性能指标改为参考量级并按 provider 区分；安全章节改为精确表述（at-rest vs 传输加密）；新增 Roadmap 章节明确未实现项（真正的向量检索、Neo4j、分布式并发）。

## ⚠️ Breaking Changes
- `MemoryScope` 现强制过滤：旧调用方依赖"scope 不隔离"行为的代码需要适配。
- 生产环境启动 MCP server 必须设置 `AMP_API_TOKEN`。
- Python 包名从历史 `amp-memory` 已统一为 `agent-memory-protocol`。

## 测试
- TypeScript: 3 suites, 65 passed, 1 skipped (Redis 未启用环境跳过)
- Python: 7/7 passed
- `npm run build`、`npm run lint`、`npm run start`、`npm run demo` 均通过验证
